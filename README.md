# AuraAI phone-build starter

This is a real source-project starter, not a compiled APK.

Build:
1. Open this folder in Android Studio.
2. Allow Gradle to sync.
3. Build > Build APK(s).

The project intentionally does not provide unrestricted OS access, self-modifying security controls, hidden microphone/screen capture, or autonomous real-money trading.
